import { describe, expect, it, vi } from 'vitest';
const clear=vi.fn();vi.mock('../../../src/renderer/modules/drag-drop/drag-drop-functions.js',()=>({holdingTankDrop:vi.fn(),allowHotkeyDrop:vi.fn(),holdingTankReorderDrop:vi.fn(),holdingTankReorderDragOver:vi.fn(),clearHoldingTankDropIndicators:clear}));vi.mock('../../../src/renderer/modules/adapters/secure-adapter.js',()=>({secureStore:{set:vi.fn()}}));
let aborted=false;globalThis.AbortController=class{constructor(){this.signal={addEventListener:vi.fn()};}abort(){aborted=true;}};const handlers={};const tank={addEventListener:(n,h)=>{handlers[n]=h;},contains:()=>false};globalThis.window={debugLog:{info:vi.fn(),warn:vi.fn(),error:vi.fn()},secureElectronAPI:{}};globalThis.document={getElementById:id=>id==='holding_tank'?tank:null,querySelectorAll:()=>[]};const drag=await import('../../../src/renderer/modules/drag-drop/event-handlers.js');
describe('drag/drop event handler lifecycle',()=>{it('delegates holding-tank listeners and aborts them during cleanup',()=>{drag.setupDragDropEventHandlers();expect(Object.keys(handlers)).toEqual(expect.arrayContaining(['drop','dragover','dragleave']));handlers.drop({stopPropagation:vi.fn(),target:{classList:{remove:vi.fn()}},dataTransfer:{getData:()=>''}});expect(clear).toHaveBeenCalled();drag.cleanupDragDropEventHandlers();expect(aborted).toBe(true);});});
