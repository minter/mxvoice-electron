import { beforeEach, describe, expect, it, vi } from 'vitest';
globalThis.window={secureElectronAPI:{analytics:{trackEvent:vi.fn()}},debugLog:{info:vi.fn(),warn:vi.fn(),error:vi.fn()}};const selected={removeAttribute:vi.fn()};const current={classList:{remove:vi.fn()},getAttribute:()=> '1',nextElementSibling:null};globalThis.document={querySelector:vi.fn(()=>current),getElementById:vi.fn(()=>selected),querySelectorAll:()=>[]};
const sharedState=(await import('../../../src/renderer/modules/shared-state.js')).default;const manager=await import('../../../src/renderer/modules/audio/audio-manager.js');
describe('audio manager sequencing',()=>{beforeEach(()=>{vi.clearAllMocks();sharedState.reset();current.nextElementSibling=null;});
it('leaves playlist mode active and clears selection at end of autoplay',async()=>{sharedState.set('autoplay',true);sharedState.set('holdingTankMode','playlist');await manager.autoplay_next();expect(current.classList.remove).toHaveBeenCalledWith('now_playing');expect(selected.removeAttribute).toHaveBeenCalledWith('id');expect(sharedState.get('holdingTankMode')).toBe('playlist');});
it('does not trigger crossfade outside the configured window',()=>{sharedState.set('crossfadeSeconds',2);sharedState.set('crossfadeTriggered',false);manager.triggerEarlyCrossfade(3);expect(sharedState.get('crossfadeTriggered')).toBe(false);manager.triggerEarlyCrossfade(2);expect(sharedState.get('crossfadeTriggered')).toBe(true);});});
