import { describe, expect, it, vi } from 'vitest';
const commands={increaseFontSize:vi.fn(),decreaseFontSize:vi.fn(),toggleWaveform:vi.fn(),toggleAdvancedSearch:vi.fn(),closeAllTabs:vi.fn(),deleteSelectedSong:vi.fn(),editSelectedSong:vi.fn(),manageCategories:vi.fn()};const state={setupWindowStateSaving:vi.fn(),saveWindowState:vi.fn(),loadWindowState:vi.fn(),prepareMainWindowForClose:vi.fn()};const lifecycle={setup:vi.fn(),isQuitting:()=>false};const createMainWindow=vi.fn(()=>({id:'window'}));const buildApplicationMenu=vi.fn(()=>['menu']);
vi.mock('electron',()=>({default:{app:{},BrowserWindow:function(){},Menu:{},dialog:{},shell:{},nativeTheme:{},screen:{},ipcMain:{}}}));vi.mock('electron-log',()=>({default:{}}));vi.mock('../../../src/main/modules/log-service.js',()=>({getLogService:vi.fn()}));vi.mock('../../../src/main/modules/profile-manager.js',()=>({}));vi.mock('../../../src/main/modules/renderer-command-dispatcher.js',()=>({createRendererCommandDispatcher:()=>commands}));vi.mock('../../../src/main/modules/application-menu.js',()=>({buildApplicationMenu}));vi.mock('../../../src/main/modules/window-state-manager.js',()=>({createWindowStateManager:()=>state}));vi.mock('../../../src/main/modules/main-window-factory.js',()=>({createMainWindow}));vi.mock('../../../src/main/modules/app-lifecycle.js',()=>({createAppLifecycle:()=>lifecycle}));
const setup=await import('../../../src/main/modules/app-setup.js');
describe('app setup orchestration',()=>{it('wires command, window-state, lifecycle, window and menu boundaries',()=>{setup.initializeAppSetup({mainWindow:{},store:{},debugLog:{},getCurrentProfile:()=> 'Default User'});setup.increaseFontSize();setup.sendDeleteSong();expect(commands.increaseFontSize).toHaveBeenCalled();expect(commands.deleteSelectedSong).toHaveBeenCalled();expect(setup.createWindow({width:900})).toEqual({id:'window'});expect(createMainWindow).toHaveBeenCalledWith(expect.objectContaining({state:expect.objectContaining({width:900})}));expect(setup.createApplicationMenu()).toEqual(['menu']);expect(buildApplicationMenu).toHaveBeenCalled();setup.setupWindowStateSaving();setup.setupAppLifecycle();expect(state.setupWindowStateSaving).toHaveBeenCalled();expect(lifecycle.setup).toHaveBeenCalled();});});
